<?xml version="1.0"?>
<odoo>
    <record id="appointment_form" model="ir.ui.view">
        <field name="name">Appointment Form</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <form string="Appointment">
                <header>
                    <button name="mark_cancel" string="Cancel Appointment" states="draft" type="object" class="oe_highlight" confirm="Are you sure to cancel this Appointment"/>
<!--                    <button name="set_open" string="Stop Recruitment" states="recruit" type="object" groups="base.group_user"/>-->
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_consultation,done" nolabel="1" options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                    <field name="priority" widget="priority"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="patient_id"/>
                            <field name="appointment_time"/>
                            <field name="date_of_birth"/>
                        </group>
                        <group>
                            <field name="booking_time"/>
                            <field name="gender"/>
                            <field name="age"/>
                            <field name="cancel_time" attrs="{'invisible': [('state', '!=', 'canceled')]}"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Prescription" name="prescription">
                            <group>
                                <field name="prescription"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="appointment_tree" model="ir.ui.view">
        <field name="name">Appointment List</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <tree>
                <field name="patient_id"/>
                <field name="appointment_time"/>
                <field name="booking_time"/>
                <field name="gender"/>
                <field name="age"/>
                <field name="date_of_birth"/>
            </tree>
        </field>
    </record>

    <record id="appointment_search" model="ir.ui.view">
        <field name="name">Appointment Search</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <search string="State">
                <filter string="Draft" name="filter_draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Done" name="filter_done" domain="[('state', '=', 'done')]"/>
                <filter string="In Consultation" name="filter_in_consultation" domain="[('state', '=', 'in_consultation')]"/>
                <filter string="Canceled" name="filter_canceled" domain="[('state', '=', 'canceled')]"/>
                <searchpanel>
                    <field name="state" string="State" enable_counters="1" context="{}"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <record id="action_appointment" model="ir.actions.act_window">
        <field name="name">Appointment</field>
        <field name="res_model">hospital.appointment</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add a new Appointment
            </p>
            <p>
                Customer is GOD and treat them properly.
            </p>
        </field>
    </record>

    <menuitem id="menu_appointment"
              name="Appointments"
              action="action_appointment"
              parent="hospital_menu_root"
              sequence="10"
    />


</odoo>
