<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="library_books.list">
      <field name="name">library_books list</field>
      <field name="model">library_books.library_books</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="library_books.action_window">
      <field name="name">library_books window</field>
      <field name="res_model">library_books.library_books</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="library_books.action_server">
      <field name="name">library_books server</field>
      <field name="model_id" ref="model_library_books_library_books"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="library_books" id="library_books.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="library_books.menu_1" parent="library_books.menu_root"/>
    <menuitem name="Menu 2" id="library_books.menu_2" parent="library_books.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="library_books.menu_1_list" parent="library_books.menu_1"
              action="library_books.action_window"/>
    <menuitem name="Server to list" id="library_books" parent="library_books.menu_2"
              action="library_books.action_server"/>
    -->
  </data>
</odoo>